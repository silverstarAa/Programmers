SELECT
    NAME
    , DATETIME
FROM (
    SELECT
        A.NAME
        , A.DATETIME
    FROM ANIMAL_INS A
    LEFT OUTER JOIN ANIMAL_OUTS B ON B.ANIMAL_ID = A.ANIMAL_ID
    WHERE B.DATETIME IS NULL
    ORDER BY A.DATETIME
)
WHERE ROWNUM <= 3
;
