  
import java.util.Arrays;

class Solution {
  public String solution(String s) {
      String ans = "";
      String[] arr = s.split("");
      Arrays.sort(arr);
      
      for (int i = arr.length - 1; i >= 0; i--) ans += arr[i];
      
      return ans;
  }
}
